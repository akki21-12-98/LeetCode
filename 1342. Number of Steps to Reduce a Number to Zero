class Solution:
    def numberOfSteps (self, num: int) -> int:
        ctr = 0
        while num > 0:
            if num % 2:
                num -= 1
            else:
                num //= 2
            ctr += 1
        return ctr
